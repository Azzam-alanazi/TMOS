# Optional — if you prefer env vars over the in-app settings dialog.
# These are NOT auto-loaded by T.M.O.S today; they're here for reference.
# The app reads keys from ~/.tmos/config.json which is created when you click
# the Settings icon (⚙) and press Save.

GROQ_API_KEY=gsk_your_groq_key_here
GEMINI_API_KEY=AIza_your_gemini_key_here
