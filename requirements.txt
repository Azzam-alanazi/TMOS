# Qt WebEngine 6.11.2 crashes (access violation in Qt6WebEngineCore.dll) within
# minutes on Windows, so stay on 6.10 until a fixed 6.11.x is out.
PyQt6>=6.4.0,<6.11
PyQt6-WebEngine>=6.4.0,<6.11
requests>=2.28.0
psutil>=5.9.0
APScheduler>=3.10.0
SpeechRecognition>=3.10.0
PyAudio>=0.2.13
# voxcpm  # Optional — requires C++ build tools on Windows/Py3.13. edge-tts is used instead.
soundfile>=0.12.1
edge-tts>=6.1.9
pygame>=2.5.0
pyttsx3>=2.90
flask>=3.0.0
flask-cors>=4.0.0
